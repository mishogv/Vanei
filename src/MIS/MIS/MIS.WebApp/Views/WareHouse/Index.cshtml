@using MIS.ViewModels.View.WareHouse
@model IndexWarehouseViewModel

@{
    ViewData["Title"] = "Index";
}

<link href="~/css/addons/datatables.min.css" rel="stylesheet">

<div class="row">
    <h1>@Model.WareHouseName</h1>
    @if (!Model.IsFavorite)
    {
        <form asp-controller="WareHouse" asp-action="Favorite" asp-route-id="@Model.Id" method="get">
            <input class="btn btn-primary ml-5" value="Make it Favorite" type="submit" />
        </form>
    }

    <a class="btn btn-primary" asp-controller="WareHouse" asp-action="Edit" asp-route-id="@Model.Id">
        Edit
    </a>
    <a class="btn btn-primary" asp-controller="WareHouse" asp-action="Delete" asp-route-id="@Model.Id">
        Delete
    </a>
</div>
<div>
    <span>Categories</span>
    <a asp-controller="Category" asp-action="Create" asp-route-id="@Model.Id">Create</a>
</div>
<div>
    <span>Products</span>
    <a asp-controller="Product" asp-action="Create" asp-route-id="@Model.Id">Create</a>
</div>

<div>Choose other warehouse</div>
<div class="dropdown">
    <a class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown"
       aria-haspopup="true" aria-expanded="false">Warehouse</a>

    <div class="dropdown-menu dropdown-primary">
        @foreach (var warehouse in Model.WarehouseDropdown)
        {
            <a asp-controller="WareHouse" asp-action="Index" asp-route-id="@warehouse.Id" class="dropdown-item">@warehouse.Name</a>
        }
    </div>
</div>

<table id="dtBasicExample" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th class="th-sm">Name</th>
            <th class="th-sm">Price</th>
            <th class="th-sm">Quantity</th>
            <th class="th-sm">Barcode</th>
            <th class="th-sm">Category</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model.Products.OrderBy(x => x.CategoryName))
        {
            <tr>
                <td>@product.Name</td>
                <td>@product.Price</td>
                <td>@product.Quantity</td>
                <td>@product.BarCode</td>
                <td>
                    <div class="d-flex container-fluid justify-content-between">
                        <div>
                            @product.CategoryName
                        </div>
                        <div>
                            <a asp-controller="Product" asp-action="Edit" asp-route-id="@product.Id">
                                <img src="~/ico/open-iconic-master/png/pencil-2x.png" class="mx-auto" />
                            </a>
                            <a asp-controller="Product" asp-action="Delete" asp-route-id="@product.Id">
                                <img src="~/ico/open-iconic-master/png/x-2x.png" class="mx-auto" />
                            </a>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts{

    <script type="text/javascript" src="js/addons/datatables.min.js"></script>
    <script src="~/js/site.js"></script>
}